.claude
.DS_Store
.vscode
/tmp
._*
.#*
*.bak
*.old
*.md
*.pdf
!README.md

# Note, the PDF wouldn't usually be included, but as it's uniquely part of
# pdf generation instruction, it's included as example.
!README.pdf

node_modules/
# Never been tracked: an engine's dependencies are installed at install time
# against whatever the user has, and a lockfile committed here would pin a
# resolution nobody verified on their platform.
package-lock.json
pdfulator.tar.gz
pdfulator.tar.gz.sha256
.dist/

.version
