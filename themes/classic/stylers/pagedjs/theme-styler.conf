# Styling for the pagedjs DOM. A separate file from the vivliostyle one rather
# than a shared sheet with exceptions: the two renderers differ in enough of
# the page-furniture details that the exceptions were most of the file.
stylesheet = +./classic.styler-pagedjs.css
