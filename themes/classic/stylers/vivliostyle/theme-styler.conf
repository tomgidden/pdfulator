# Styling for the vivliostyle DOM. Shared by `vivlio` and `vivlio-docker`,
# which are the same renderer and would otherwise need two identical copies.
template.styling = +./classic.styler-vivliostyle.css
